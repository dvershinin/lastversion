sonar.projectKey=dvershinin_lastversion
sonar.organization=dvershinin

# Suppress lock-file rule on pyproject.toml.
# lastversion is a setuptools-based library installed via `pip install lastversion`
# from PyPI; end users do not consume our lock file. Pinning transitive
# dependencies in a library project would harm downstream dependency
# resolution. This is a library, not a deployable application.
sonar.issue.ignore.multicriteria=e1
sonar.issue.ignore.multicriteria.e1.ruleKey=text:S8565
sonar.issue.ignore.multicriteria.e1.resourceKey=pyproject.toml
